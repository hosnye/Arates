# No secrets are required to run the app — domino scanning runs entirely on-device
# in the browser (the trained model at public/models/domino.onnx). This file is kept
# only as a placeholder for any future environment variables.

# APP_URL: optional — the public URL where this app is hosted.
APP_URL="MY_APP_URL"
